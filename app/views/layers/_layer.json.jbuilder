json.extract! layer, :id, :name, :layer, :show_legend, :type, :source, :srid, :display, :primitive, :imagery, :style_id, :ordem, :map_id, :created_at, :updated_at
json.url layer_url(layer, format: :json)
